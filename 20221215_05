#include <stdio.h>
#include <stdlib.h>

int main() {
  float area, lado;
  printf("Qual e a medida do lado: ");
  scanf("%f",&lado);
  area = lado * lado;
  printf("o doblo da area e %.2f", area*2);
    return 0;
}
