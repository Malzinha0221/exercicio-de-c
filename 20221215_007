#include <stdio.h>
#include <stdlib.h>

int main() {
  float F, C;
  
  printf("temperatura em fahrenheit: ");
  scanf("%f",&F);
  C = (5 * (F-32) / 9);
  printf("A temperatura em celsius e: %.2f", C); 
    return 0;
}
