#include <stdio.h>
#include <stdlib.h>

int main() {
  float F, C;
  
  printf("temperatura celsius: ");
  scanf("%f",&C);
  F = (C * 1.8) +32;
  printf("A temperatura em fahrenheit e: %.2f", F); 
    return 0;
}
