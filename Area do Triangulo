#include <stdio.h>
int main() {    
  float base, altura;  
  printf("Descreva a base do triangulo: ");
  scanf("%f", &base);
  printf("Descreva a altura do triangulo: ");
  scanf("%f", &altura);
  printf("a area do triangulo e: %.2f",(base*altura)/2);
  system("pause");
  return 0;
